/* Redesign Market page. Rendered inside <AppShell> (which provides the .rd
 * wrapper + header). Section components get added in subsequent tasks. */
export default function MarketPage() {
  return (
    <div className="body">
      <div style={{ padding: '40px 0', color: 'var(--ink-3)', fontFamily: 'var(--mono)' }}>
        Market page — sections coming next.
      </div>
    </div>
  )
}
